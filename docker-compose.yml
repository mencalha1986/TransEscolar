services:
  # db:
  #   image: postgres:17
  #   environment:
  #     POSTGRES_DB: transporte_escolar
  #     POSTGRES_USER: postgres
  #     POSTGRES_PASSWORD: postgres
  #     POSTGRES_INITDB_ARGS: "--locale-provider=libc --lc-collate=C --lc-ctype=C"
  #   volumes:
  #     - pgdata:/var/lib/postgresql/data
  #     - ./docker/init.sql:/docker-entrypoint-initdb.d/init.sql:ro
  #   ports:
  #     - "5433:5432"
  #   healthcheck:
  #     test: ["CMD-SHELL", "pg_isready -U postgres"]
  #     interval: 5s
  #     timeout: 5s
  #     retries: 10

  api:
    build: .
    ports:
      - "8080:8080"
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_URLS: http://+:8080
      ConnectionStrings__DefaultConnection: "Host=aws-1-us-east-2.pooler.supabase.com;Port=5432;Database=postgres;Username=postgres.mmdzbcdgzoldaegaejlv;Password=aPbEUkyyxfMLHD6m;SSL Mode=Require;Trust Server Certificate=true"
      Jwt__Key: "super-secret-key-change-in-production-min-32-chars"
      Jwt__Issuer: "TransporteEscolar"
      Jwt__Audience: "TransporteEscolar"
      Jwt__ExpiresHours: "8"

  frontend:
    build: ./FrontEnd
    ports:
      - "3000:80"
    depends_on:
      - api

# volumes:
#   pgdata:
